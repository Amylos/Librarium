
import '../styles/settings.css';

const Settings = (props) => {
    console.log("Stee")

    return (
        <div className="Settings">
            Settings
        </div>
      );
}

export default Settings;